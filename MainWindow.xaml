<Window x:Class="WPF_WindowSizeChange.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_WindowSizeChange" d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <ComboBox SelectedItem="{Binding TaskComboBox}" Name="TaskComboBox" Grid.ColumnSpan="3" Grid.Row="0"></ComboBox>

        <TextBox Name="WidthBox" TextAlignment="Center" Grid.Column="0" Grid.Row="1"></TextBox>
        <TextBox Name="HeightBox" TextAlignment="Center" Grid.Column="1" Grid.Row="1"></TextBox>
        <Button Name="RunButton" Command="{Binding ButtonCommand.ChangeCommand}" Content="実行" Grid.Column="2" Grid.Row="1"></Button>
    </Grid>
</Window>
